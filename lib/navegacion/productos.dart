import 'package:flutter/material.dart';

class Productspage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("PRODUCTOSSS"),
      ),
    );
  }
}
