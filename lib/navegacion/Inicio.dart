import 'package:flutter/material.dart';

class inipage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("INICIOOOOO"),
      ),
    );
  }
}
